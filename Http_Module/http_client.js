/*
    *** HTTP Client ***
        => Anything that sends an HTTP request to the server and receives a response,
            it's called HTTP Client.

        *** Example of HTTP Client
            => Axios, Jquery Ajax, Fetch (JavaScript)
            => cUrl (Php)
            => Volly, Retrofit ( Java )
            => RestSharp (Dot Net)

        *** HTTP Client For Testing
            => Postman
            => Fiddler

    => We can be requested several way:
            * Get Request
            * Post Request
            * The parameter of Request
            * The data pass of Header
            * The data pass of Body
            * JSON Request
            * Multiplatform Request
*/
